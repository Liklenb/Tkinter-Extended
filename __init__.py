import os
import sys
sys.path.append(os.path.dirname(os.path.abspath(__file__)))
from widget import (Size, EdgeInsets, Widget, StatefulWidget, MainAxisAlignment, CrossAxisAlignment, Text, FileImage,
                    Expanded, Column, Row, Center, Container, InkWell, FilledButton, Icon, IconButton, ListView,
                    BoxConstraints)
from window import AppSettings, run_app, Navigator
from color import Color, Colors
from icon import Icons